# Get the git root directory dynamically
GIT_ROOT=$(git rev-parse --show-toplevel)

# Remove the .gitignore in the native app folder relative to the git root
rm -rf "$GIT_ROOT/apps/native/.gitignore"

pnpm encrypt:all

git add .

# lint-staged
lint-staged
